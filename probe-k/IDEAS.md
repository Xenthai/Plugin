# Ideas

Raw, unfiltered, and deliberately not a roadmap. An item here has not been argued through yet —
`ROADMAP.md` is where something goes once it has a reason, a cost and a thing it waits on, and
`ROADMAP.md § Deliberately not on the roadmap` is where it goes when the answer turned out to be no.

Nothing is pending here right now.

## Answered

- **Detect optimisation opportunities from the setup, the continuous use of AI and the journal.**
  Shipped as the `opportunities` capability: `bin/opportunities.mjs` reports what recurred across
  distinct periods with the rows behind each pattern, and `skills/opportunities/SKILL.md` carries
  the judgement half. The doctrine is `capabilities/process/doctrine/PROCESS.md` §8, whose core
  claim is that after a few periods the journal beats the interview — it replaces the two
  suitability criteria normally filled from recall with measured values.
